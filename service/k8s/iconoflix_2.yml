apiVersion: v1
kind: Pod
metadata:
  name: iconoflix2
spec:
  containers:
    - name: iconoflix
      image: quay.io/imhotepio/iconoflix:mem
      ports:
        - name: api
          containerPort: <<!!CHANGE_ME!!>>
