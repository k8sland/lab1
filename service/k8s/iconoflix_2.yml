apiVersion: v1
kind: Pod
metadata:
  name: iconoflix2
spec:
  containers:
    - name: iconoflix
      image: quay.io/imhotep/iconoflix:mem
      ports:
        - name: api
          containerPort: <<!!CHANGE_ME!!>>
