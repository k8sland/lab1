apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: iconoflix
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: iconoflix
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 5
